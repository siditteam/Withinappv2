export const validationPackageName = "@within/validation" as const;

export type ContentStatus = "draft" | "scheduled" | "published" | "archived";
export type ContentVisibility = "free" | "premium" | "inner_circle" | "invite_only";
